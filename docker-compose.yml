services:
  app:
    build: .
    ports:
      - "8001:8000"
    env_file:
      - backend/.env
    volumes:
      # Persist SQLite database
      - db-data:/app/backend/data
    restart: unless-stopped

volumes:
  db-data:
